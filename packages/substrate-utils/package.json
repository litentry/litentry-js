{
  "name": "@litentry/substrate-utils",
  "version": "0.0.0",
  "description": "JS utilities for use with substrate based dApps",
  "main": "dist/index.js",
  "types": "dist/types/index.d.ts",
  "license": "Apache-2.0",
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "watch": "tsc --watch",
    "postinstall": "yarn build"
  },
  "dependencies": {
    "@polkadot/api": "^6.5.2",
    "@polkadot/extension-dapp": "^0.40.4"
  }
}
